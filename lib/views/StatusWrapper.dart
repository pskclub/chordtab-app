import 'package:flutter/cupertino.dart';

class StatusWrapper extends StatelessWidget {
  const StatusWrapper({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
